---
name: session-manager
description: 打开 Codex 会话管理页，查看会话所在项目与文件线索，并归档或永久删除会话。
---

# Codex 会话管理

当用户要求查看、整理、归档或删除 Codex 会话时使用本插件。

1. 优先调用 `open_session_manager`，让用户在可视化管理页中选择会话。若返回的是文本会话列表而不是管理页，说明当前客户端渲染不了组件；此时如果用户想批量归档或删除，改调 `select_sessions` 引导用户完成筛选、选择和操作。`select_sessions` 返回的 `interactive.performed` 表示操作已由用户在表单中确认并执行完毕，只需如实转述结果，不要再调用 `archive_sessions` 或 `delete_sessions` 重复执行。客户端能正常显示管理页时不要调用 `select_sessions`。
2. 用户只想查找项目或文件时，可调用 `list_sessions` 和 `inspect_session_files`。
   `list_sessions` 支持按类别标签筛选，也支持按最后更新时间筛选 1 天内、1 周内、1 个月内、3 个月前、1 个月前、1 周前，或传入自定义开始/结束日期。
3. 永久删除必须由用户在管理页中选择会话；中文界面输入精确确认词 `删除`，英文界面输入 `delete`。
   在 Codex CLI 等无管理页的客户端上，插件会在删除前自行弹出勾选表单，最终名单以用户勾选结果为准；若返回结果显示已取消或未勾选，不要重试或改用其他方式删除。
4. 不得替用户猜测删除目标，也不得绕过当前会话保护。
5. 说明清楚：删除的是 Codex 持久化会话及其派生会话，不会删除项目文件，也不会清理跨会话共享的插件缓存、模型缓存或凭据。
6. 永久删除成功后插件会同步桌面侧边栏；若页面提示同步失败，告知用户刷新或重启 Codex，不要再次删除同一会话。
7. 用户希望留在终端、不打开桌面端管理页时，告知可运行插件目录下的 `scripts/launch_tui.sh` 使用终端界面；它复用同一套安全规则，但由用户自己操作，不要代为执行删除。
8. 标记为“隐藏分叉”的会话虽然不在 Codex 常规列表中，但仍可能引用并阻止源会话删除。必须让用户选择该分叉；若分叉与源会话同时选择，按分叉在前、源会话在后的顺序删除。不得仅因存在引用就自动扩大删除目标。
